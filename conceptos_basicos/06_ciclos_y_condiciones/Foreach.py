# Usando el ciclo for (como un foreach) para iterar sobre colecciones

usuarios = ['Chanchito Feliz', 'Felipe', 'Roberto', 'Nicolas']

for usuario in usuarios: # el for se utiliza cuando se conoce el numero de iteraciones
    print(usuario)